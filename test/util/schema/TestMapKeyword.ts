import { expect } from 'chai';

import { entityMeta } from '../../src/util/logger';
import { makeTestActor, makeTestItem, makeTestPortal, makeTestRoom } from '../entity';

describe('schema map keyword', () => {
  it('should fail if any key fails');
  it('should fail if any value fails');
  it('should collect errors from all items');
});

