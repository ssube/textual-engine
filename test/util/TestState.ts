describe('state utils', () => {
  describe('search state helper', () => {
    xit('should find entities whose type matches');
    xit('should find entities whose metadata matches');
    xit('should skip entities whose room does not match');
    xit('should not include entities more than once');
  });

  describe('search state string helper', () => {
    xit('should find entities whose ID matches');
    xit('should find entities whose name matches');
    xit('should not include entities more than once');
  });
});
